{
  "name": "@adhesivejs/monorepo",
  "version": "0.2.1",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adhesivejs/adhesive.git"
  },
  "scripts": {
    "automd": "pnpm -r automd",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "playground:core": "pnpm --filter @adhesivejs/playground-core run dev",
    "playground:react": "pnpm --filter @adhesivejs/playground-react run dev",
    "playground:vue": "pnpm --filter @adhesivejs/playground-vue run dev",
    "release": "pnpm release:bump && pnpm build && pnpm automd && git add . && changelogen --release --push && pnpm release:publish",
    "release:bump": "pnpm -r release:bump",
    "release:publish": "pnpm -r release:publish",
    "test": "pnpm build && pnpm lint && pnpm type-check && pnpm test:unit && pnpm test:attw && pnpm test:publint",
    "test:unit": "pnpm --filter @adhesivejs/test run test",
    "test:attw": "pnpm -r test:attw",
    "test:publint": "pnpm -r test:publint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@danielwaltz/eslint-config": "catalog:",
    "@tsconfig/node22": "catalog:",
    "@types/node": "catalog:",
    "automd": "catalog:",
    "changelogen": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "publint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
