{
  "name": "@adhesivejs/monorepo",
  "type": "module",
  "version": "0.7.9",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adhesivejs/adhesive.git"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "playground:core": "pnpm -F @adhesivejs/playground-core run dev",
    "playground:react": "pnpm -F @adhesivejs/playground-react run dev",
    "playground:svelte": "pnpm -F @adhesivejs/playground-svelte run dev",
    "playground:vue": "pnpm -F @adhesivejs/playground-vue run dev",
    "release": "pnpm release:bump && pnpm release:prepare && pnpm release:publish",
    "release:bump": "pnpm -r release:bump && pnpm build",
    "release:prepare": "git add . && changelogen --release --push",
    "release:publish": "pnpm -r release:publish",
    "test": "pnpm build && pnpm lint && pnpm type-check && pnpm test:unit && pnpm test:publish",
    "test:unit": "pnpm -F @adhesivejs/test run test",
    "test:publish": "pnpm -r test:publish",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@danielwaltz/eslint-config": "catalog:",
    "@tsconfig/node24": "catalog:",
    "@types/node": "catalog:",
    "changelogen": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "publint": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.9.0"
    },
    "runtime": {
      "name": "node",
      "version": "24.18.0"
    }
  }
}
